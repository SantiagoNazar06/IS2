<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Registrar Carrera</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Registrar Carrera</h1>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        <form action="/register_career" method="post" class="space-y-4">
            <div>
                <label for="career_name" class="block text-gray-700 text-sm font-medium mb-1">Nombre de la Carrera:</label>
                <input type="text" id="career_name" name="career_name" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500">
            </div>
            <div>
                <label for="career_duration" class="block text-gray-700 text-sm font-medium mb-1">Duración (años):</label>
                <input type="number" id="career_duration" name="career_duration" min="1" required
                       class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500">
            </div>
            
            <button type="submit"
                    class="w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                Registrar Carrera
            </button>
            
            <div class="flex justify-between items-center mt-4 text-sm">
                <a href="/dashboard" class="text-gray-600 hover:underline">Volver al inicio</a>
                <a href="/careers" class="text-indigo-600 font-semibold hover:underline">Ver todas las carreras</a>
            </div>
        </form>
    </div>
</body>
</html>